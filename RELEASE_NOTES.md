**Fledge Ver.0.5.0**（第3世代・Tauri シェル / Latest）

第3世代の最初の公開版です。シェルを **Electron → Tauri 2 + Rust** に移行し、インストーラを大幅に軽量化しました。  
`0.x.x` はベータ、`1.0.0` 以降を正式版とするバージョン方針に切り替えます（接尾辞 `a` / `b` / `r` / `rc` などは使いません）。

> **Ver.0.4.6 からの自動更新はありません**（第2世代の世代ロック）。0.5 系は本インストーラから新規インストールしてください。ログイン状態は 0.4 の暗号化形式と非互換のため、**再ログインが必要**です。

## 技術・アーキテクチャ

- シェルを **Tauri 2 + Rust** に移行（UI は React のまま）
- ランチャー中核を `crates/fledge-core`（Rust）へ集約
- Chromium 同梱をやめ **WebView2** を利用 → 配布サイズが大幅に縮小（0.4 系約 100MB 前後 → 0.5 系おおよそ数 MB〜十数 MB 級）
- IPC は `fledge_invoke` 経由で既存 `window.fledge` API 面を維持
- 認証トークンは **Windows DPAPI** で端末内暗号化（0.4 の Electron `safeStorage` データは読めないため再ログイン）
- 配布物は **`Fledge_0.5.0_x64-setup.exe`**（インストール先に `Fledge.exe` と `uninstall.exe`）
- Electron 経路は非推奨（参照用ソースのみ）

## 主な機能・UI

- ナビゲーションに **ギャラリー**（全インスタンスのスクリーンショット横断表示）を追加
- スキン編集で **公式ケープ** の一覧・選択に対応
- デフォルトスキン PNG を製品版に同梱（プレビュー欠落を解消）
- **アプリ設定のインポート／エクスポート**（`option.flg` テキスト）。インスタンスデータは含みません
- **バックアップ機能を削除**（上記 `option.flg` で設定の持ち運びを代替）
- UI サイズを整理: コンパクト / **ノーマル（既定）** / ラージ / ワイド。基準見た目をノーマル＝表示倍率 1.0（内部 zoom 基準 0.925）に再校正
- スタートアップアニメーションとその設定を削除
- 設定タブなどのレイアウト・スイッチ位置ずれ・キーバインド注意文などを製品向けに修正
- UI 言語を複数言語に拡充（既定は日本語。一部はベータ翻訳）

## 起動・コンテンツ（継続）

- Vanilla / Fabric / Forge / NeoForge / Quilt、スナップショット
- 新規インスタンス初回のゲーム初期設定（言語・映像・音声・操作）
- Modrinth からの Mod / リソースパック / シェーダー等の検索・導入
- Microsoft 複数アカウント、スキン管理、お知らせ、アプリ内更新、アンインストール

## バージョン方針（今後）

| 範囲 | 扱い |
|------|------|
| `0.x.x` | ベータ |
| `1.0.0` 以降 | 正式版 |
| 接尾辞 | 使わない（`a` / `b` / `r` / `rc` / `ut` など） |

タグ・配布名は `v0.5.0` / `Fledge_0.5.0_x64-setup.exe`、表示は `Ver.0.5.0` です。

不具合や要望は [GitHub Issues](https://github.com/arula-folne/Fledge/issues) からお知らせください。
