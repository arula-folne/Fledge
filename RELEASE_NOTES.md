**Fledge Ver.0.5.17**

インスタンス周りと初期設定の不具合を修正しました。

## 修正

- **構成の複製**: 複製後に詳細画面へ遷移せず、ホームのままにする
- **ログ画面**: ログ量に応じて伸びず、最初からウィンドウ下端付近まで表示する
- **詳細の開始ボタン**: 準備中に楕円ボタンへ円形リングを重ねない（グレー無効表示のみ）
- **Minecraft 初期設定**: 「すべてリセット」で操作設定（キーバインド）が残る問題を修正
- **初期設定リセット後の起動**: 変更なしのときは古い `options.txt` を消して MC 既定で起動する
- **インスタンス複製**: `options.txt` / `debug.json` をコピーしない
- **同名インスタンス**: フォルダ名を ID の正本にし、ASCII の一意 ID で必ず別インスタンスとして扱う

## 配布

- タグ: `v0.5.17`
- インストーラ: **`Fledge_0.5.17_x64-setup.exe`**
- 表示: `Ver.0.5.17`

不具合や要望は [GitHub Issues](https://github.com/arula-folne/Fledge/issues) からお知らせください。
