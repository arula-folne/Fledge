# Fledge local environment (DO NOT COMMIT real secrets)
# Copy to `.env` and fill values. `.env` is gitignored.
#
# CurseForge is currently disabled in the app (CURSEFORGE_FEATURE_ENABLED=false).
# When re-enabled, set your own key here. Never commit or push the real value.

# FLEDGE_CURSEFORGE_API_KEY=
